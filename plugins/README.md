# Plugins

Place each concrete plugin in its own subdirectory.

These directories are for plugin packages, not for catalog ideas and not for platform runtime module records.

Example:

```text
plugins/
  my-new-plugin/
  recommendation-engine/
```

Each plugin directory is expected to be scaffolded from `templates/plugin-template` in this repository.
